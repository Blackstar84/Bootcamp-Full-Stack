let autos = ['mazda', 'tesla', 'audi', 'toyota'];


for (let contador = 0; contador < autos.length; contador++ ){
    // console.log('el elemento es: ',autos[contador], 'coon la posicion ', contador);
    console.log(`el elemento es ${autos[contador]} con la posicion ${contador}`);
}